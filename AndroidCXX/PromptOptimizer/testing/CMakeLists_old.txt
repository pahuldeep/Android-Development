cmake_minimum_required(VERSION 3.16)

project(PromptOptimizer VERSION 1.0 LANGUAGES CXX)

set(CMAKE_CXX_STANDARD 17)
set(CMAKE_CXX_STANDARD_REQUIRED ON)

# Find Qt6 required packages
find_package(Qt6 REQUIRED COMPONENTS Quick Gui Qml)

qt_standard_project_setup()

# Define the QML Module
qt_add_executable(prompt_optimizer_app main.cpp)

set_source_files_properties(styles/Colors.qml PROPERTIES QT_QML_SINGLETON_TYPE TRUE)
set_source_files_properties(styles/Typography.qml PROPERTIES QT_QML_SINGLETON_TYPE TRUE)
set_source_files_properties(styles/Layouts.qml PROPERTIES QT_QML_SINGLETON_TYPE TRUE)

# # --- CRITICAL: Register Singletons ---
# # This allows you to import "PromptOptimizer" and use Colors.xxx directly
# qt_target_qml_sources(prompt_optimizer_app
#     PREFIX PromptOptimizer/styles
#     QML_FILES
#         styles/Colors.qml
#         styles/Typography.qml
#         styles/Layouts.qml
# )

# # Add JS files as resources so they can be imported
# qt_target_qml_sources(prompt_optimizer_app
#     PREFIX PromptOptimizer/utils
#     QML_FILES
#         # utils/Theme.js
#         utils/Helpers.js
# )

qt_add_qml_module(prompt_optimizer_app
    URI PromptOptimizer
    VERSION 1.0
    QML_FILES
        main.qml
        pages/BasicPage.qml
        pages/AdvancedPage.qml
        pages/SettingsPage.qml

        # components/PromptInput.qml
        # components/TechniqueList.qml
        # components/AgentSelector.qml
        # components/SettingsPanel.qml
        # components/SavedPrompts.qml

        components/AgentCard.qml
        components/BottomNav.qml
        components/TechniqueRow.qml
        components/ActionButtons.qml
        components/ToggleSwitch.qml
        components/CheckOption.qml
        components/FocusOption.qml

        models/AgentModel.qml
        models/TechniqueModel.qml
        models/PromptModel.qml

        styles/Colors.qml
        styles/Typography.qml
        styles/Layouts.qml

    RESOURCES
        # You can add icons/fonts here later, e.g., assets/icons/icon.png
        utils/Helpers.js
)


target_link_libraries(prompt_optimizer_app PRIVATE Qt6::Quick Qt6::Gui)